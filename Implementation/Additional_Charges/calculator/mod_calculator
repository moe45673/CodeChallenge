class ModificationCalculator
  include Calculator

  def initialize(tax_config)
    @modifications = tax_config["modifications"].group_by { |m| m["affected_tax"] }
  end

  def calculate(item)
    
    
  end

end